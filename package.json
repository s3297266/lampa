{
  "name": "lampa-hdrezka-plugin",
  "version": "1.0.0",
  "description": "HDRezka plugin for Lampa",
  "main": "dist/plugin.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
